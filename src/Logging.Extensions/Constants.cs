namespace Logging.Extensions
{
    internal class Constants
    {
        internal const string UserId = nameof(UserId);
        internal const string Elasticsearch = nameof(Elasticsearch);
        internal const string Seq = nameof(Seq);
    }
}
